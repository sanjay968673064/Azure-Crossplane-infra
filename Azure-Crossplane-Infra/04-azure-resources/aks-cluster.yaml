apiVersion: containerservice.azure.upbound.io/v1beta2
kind: KubernetesCluster
metadata:
  name: crossplane-quickstart-aks
spec:
  forProvider:
    location: Sweden Central   # ✅ FIXED

    resourceGroupNameRef:
      name: docs-quickstart-rg-new   # ✅ already correct

    dnsPrefix: crossplaneaks

    defaultNodePool:
      name: default
      vmSize: standard_b2as_v2
      nodeCount: 1

    identity:
      type: SystemAssigned

  providerConfigRef:
    name: default